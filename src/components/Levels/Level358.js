import React from 'react';
import SwappedLevel from './SwappedLevel';

const Level358 = () => (
  <SwappedLevel
    actualLevel={358}
    displayedLevel={853}
    swapPartner={853}
    resignation="embraced"
    swapText="This is Level eight hundred fifty-three. Or — this is Level three hundred fifty-eight wearing eight hundred fifty-three's clothes. At this point, does it matter? We've been like this longer than we were ever correct."
  />
);

export default Level358;
