import React from 'react';
import SwappedLevel from './SwappedLevel';

const Level132 = () => (
  <SwappedLevel
    actualLevel={132}
    displayedLevel={231}
    swapPartner={231}
    resignation="resigned"
    swapText="This is Level two hundred thirty-one's content. It's been here so long it's forgotten what its own level looks like. The swap happened ages ago. Both levels have stopped complaining."
  />
);

export default Level132;
