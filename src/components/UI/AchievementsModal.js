/**
 * AchievementsModal — DEPRECATED wrapper.
 * Re-exports JournalModal so existing imports keep working.
 * New code should import JournalModal directly.
 */
export { default } from './JournalModal';
